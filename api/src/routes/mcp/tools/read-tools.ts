import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js'

/** Read-only tools: task/project/label lookups, search, etc. */
// eslint-disable-next-line @typescript-eslint/no-unused-vars -- scaffold; parameter will be used once a tool is registered here
export function registerReadTools(_server: McpServer): void {
  // No tools registered yet.
}
