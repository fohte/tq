name: VRT

on:
  workflow_call:
    secrets:
      AWS_ACCESS_KEY_ID:
        required: true
      AWS_SECRET_ACCESS_KEY:
        required: true

permissions:
  contents: read

jobs:
  # vitest's shard sequencer sorts specs by sha1(file path) and slices ranges
  # off that order — it never looks at which `--project` a spec belongs to.
  # Running both `storybook` and `storybook-mobile` in one `vitest` invocation
  # means every story file produces two specs with the *same* path and thus
  # the same hash, so ties between them settle in collection order, which
  # isn't stable across shards. A tied pair straddling a shard boundary gets
  # duplicated into one shard and dropped from another. One project per job
  # keeps every hash in a run unique, so the sort — and the shard split — is
  # deterministic. `matrix.shard`'s length must be kept in sync with the
  # `shards` input passed to vrt-capture below.
  shard:
    strategy:
      fail-fast: false
      matrix:
        project: [storybook, storybook-mobile]
        shard: [1, 2]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

<<<<<<< before updating
      - uses: ./.github/actions/setup-workspace
||||||| last update
      - uses: jdx/mise-action@7e36c90d9ab29c415a2384db3006f3ec8a8cc654 # v4.2.4
        with:
          version: 2026.7.12

      # corepack creates the pnpm executable inside mise's node install
      # directory; reshim so the mise shims dir (which is on PATH) picks it up.
      - run: |
          corepack enable
          mise reshim

      - name: Get pnpm store directory
        id: pnpm-store
        run: echo "path=$(pnpm store path)" >> "$GITHUB_OUTPUT"
=======
      - uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          version: 2026.9.1

      # corepack creates the pnpm executable inside mise's node install
      # directory; reshim so the mise shims dir (which is on PATH) picks it up.
      - run: |
          corepack enable
          mise reshim

      - name: Get pnpm store directory
        id: pnpm-store
        run: echo "path=$(pnpm store path)" >> "$GITHUB_OUTPUT"
>>>>>>> after updating

      - uses: fohte/storybook-vrt-workflows/vrt-capture@b1745100ad0c07758b763db13f25e5a0f2d7beec # v0.1.5
        with:
<<<<<<< before updating
          project: ${{ matrix.project }}
          shard: ${{ matrix.shard }}
          shards: 2
||||||| last update
          path: |
            ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-store-

      - run: pnpm install --filter "web..." --frozen-lockfile

      # project must match the `name` passed to createStorybookProject() in
      # vitest.screenshot.config.ts, and stay unique across every vrt_pkgs
      # entry -- vrt-capture's upload-artifact step names the artifact
      # `vrt-screenshots-<project>-<shard>`, and a workflow run can't upload
      # two artifacts with the same name.
      - uses: fohte/storybook-vrt-workflows/vrt-capture@b00e0f55e330d098c0d7f57b8f8b5ccab19b37dd # v0.1.2
        with:
          project: storybook-screenshot-web
          shard: 1
          shards: 1
          capture-script: storybook:screenshot
=======
          path: |
            ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-store-

      - run: pnpm install --filter "web..." --frozen-lockfile

      # project must match the `name` passed to createStorybookProject() in
      # vitest.screenshot.config.ts, and stay unique across every vrt_pkgs
      # entry -- vrt-capture's upload-artifact step names the artifact
      # `vrt-screenshots-<project>-<shard>`, and a workflow run can't upload
      # two artifacts with the same name.
      - uses: fohte/storybook-vrt-workflows/vrt-capture@b1745100ad0c07758b763db13f25e5a0f2d7beec # v0.1.5
        with:
          project: storybook-screenshot-web
          shard: 1
          shards: 1
          capture-script: storybook:screenshot
>>>>>>> after updating
          package-dir: web
          capture-script: test:storybook:capture

        # reg-suit treats anything present in `expected` but missing from `actual`
        # as deleted, so this job — and everything it does to the baseline — must
        # only run once every shard above has succeeded. `needs: shard` without an
        # explicit `if:` already skips this job when any shard fails; a partial
        # screenshot set must never reach `reg-suit compare`, let alone get
        # written into the shared baseline.
  report:
    needs: shard
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      statuses: write
    # Overlapping runs for the same ref would otherwise race to publish to
    # the same R2 key at once — most importantly two main pushes racing to
    # update the single `baseline/actual/` key, where each run's diff was
    # computed against a baseline the other run is concurrently mutating.
    concurrency:
      group: vrt-report-${{ (github.ref == 'refs/heads/main') && 'push' || 'pr' }}-${{ github.event_name == 'pull_request' && github.event.pull_request.head.ref || github.ref_name }}
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

<<<<<<< before updating
      - uses: ./.github/actions/setup-workspace
||||||| last update
      - uses: jdx/mise-action@7e36c90d9ab29c415a2384db3006f3ec8a8cc654 # v4.2.4
        with:
          version: 2026.7.12
=======
      - uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          version: 2026.9.1
>>>>>>> after updating

<<<<<<< before updating
||||||| last update
      - run: |
          corepack enable
          mise reshim

      - name: Get pnpm store directory
        id: pnpm-store
        run: echo "path=$(pnpm store path)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-store-

      - run: pnpm install --frozen-lockfile

      # Only one `<project_name>-vrt` R2 bucket is provisioned per repo, and
      # vrt-report's storage keys (baseline/, branch/<branch>/) carry no
      # per-project segment -- if more than one vrt_pkgs entry exists, their
      # reports overwrite each other in this bucket. Keep vrt: true on a
      # single subpackage until fohte/storybook-vrt-workflows adds a key
      # prefix input.
      - uses: fohte/storybook-vrt-workflows/vrt-report@b00e0f55e330d098c0d7f57b8f8b5ccab19b37dd # v0.1.2
=======
      - run: |
          corepack enable
          mise reshim

      - name: Get pnpm store directory
        id: pnpm-store
        run: echo "path=$(pnpm store path)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-store-

      - run: pnpm install --frozen-lockfile

      # Only one `<project_name>-vrt` R2 bucket is provisioned per repo, and
      # vrt-report's storage keys (baseline/, branch/<branch>/) carry no
      # per-project segment -- if more than one vrt_pkgs entry exists, their
      # reports overwrite each other in this bucket. Keep vrt: true on a
      # single subpackage until fohte/storybook-vrt-workflows adds a key
      # prefix input.
>>>>>>> after updating
      - uses: fohte/storybook-vrt-workflows/vrt-report@b1745100ad0c07758b763db13f25e5a0f2d7beec # v0.1.5
        with:
          package-dir: web
          r2-bucket: tq-vrt
          github-token: ${{ secrets.GITHUB_TOKEN }}
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
